# @zuu/client 更新日志

## 0.1.0 - 2026-08-13

- 首个可独立构建版本，提供类型化 HTTP/SSE client 和协议 DTO。
- 覆盖 health、auth、audit events、diagnostics、models、packages、projects、sessions、runs、workflows、schedules 和 approvals。
- 支持 prompt SSE、daemon event stream 自动重连、run event replay、model smoke test 和本地多 token 管理。
