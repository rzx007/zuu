{
  "name": "zuu",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "check": "bun --print \"await import('./src/index.ts').then(() => 'ok')\""
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "hono": "^4.13.1",
    "typebox": "^1.3.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
